export enum TicketStatus {
    ACTIVE = 'ACTIVE',
    RESOLVED = 'RESOLVED',
    TERMINATED = 'TERMINATED',
}

export enum TicketPriority {
    LOW = 'LOW',
    MEDIUM = 'MEDIUM',
    HIGH = 'HIGH',
    CRITICAL = 'CRITICAL',
}
